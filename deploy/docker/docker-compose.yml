services:
  fieldsight:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    image: fieldsight-web:latest
    container_name: fieldsight-web
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - PORT=8080
